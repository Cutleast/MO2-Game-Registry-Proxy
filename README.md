# MO2 Game Registry Proxy

A plugin for Mod Organizer 2 that changes the registry key of the game folder to match the one used by this instance when an executable is started.
The key is reverted when the executable is stopped.

